import { PictureNamePipe } from './picture-name.pipe';

describe('PictureNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PictureNamePipe();
    expect(pipe).toBeTruthy();
  });
});
